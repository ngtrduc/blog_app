<h1>
	<%= image_tag(current_user.avatar.url(:thumb), class: "img-circle avatar",height: "50", width: "50") %>
	<%= current_user.name %>
</h1>
<span><%= link_to "view my profile", current_user %></span>
<span><%= pluralize(current_user.posts.count, "post") %></span>